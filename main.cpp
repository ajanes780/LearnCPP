#include <iostream>
#include <cmath>
using namespace std;

int main() {
// Calculate the area of a circule
    float pi = 3.14;
    float radius = 5.55;

    double area = pi * pow(radius, 2);

    cout  << "the area is equal to " << area;

    return 0;
}
